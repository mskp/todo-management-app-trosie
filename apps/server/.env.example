# Allowed origin for CORS (e.g., a specific domain or localhost for development)
# Example: http://localhost:3000 or https://yourdomain.com
ALLOWED_ORIGIN="http://localhost:3000"

# Database connection string (use your database URL here)
# Example for PostgreSQL: postgres://user:password@localhost:5432/mydatabase
DATABASE_URL="postgres://user:password@localhost:5432/mydatabase"

# Secret key used for signing JWT tokens (keep this value secure)
# Example: A random string generated for your application
JWT_SECRET="your_random_secret_key"

# GitHub token for accessing GitHub API (used for integrations or automation)
# Example: Your GitHub personal access token or OAuth token
GITHUB_TOKEN="your_github_access_token"
